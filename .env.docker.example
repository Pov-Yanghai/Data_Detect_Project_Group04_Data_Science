# Copy this file to .env before running docker compose.
# This URL is baked into the frontend build at image build time.
NEXT_PUBLIC_API_URL=http://localhost:5000/api

# Optional host ports
FRONTEND_PORT=3000
BACKEND_PORT=5000
ML_SERVICE_PORT=8000
