# ML Service Production Environment
# Copy to .env and update values before deploying

NODE_ENV=production
PORT=8000

# Set higher concurrency for production (adjust based on container resources)
WEB_CONCURRENCY=4
